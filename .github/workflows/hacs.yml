name: hacs

on:
  push:
  pull_request:
  schedule:
    - cron: "30 4 * * *"

jobs:
  hacs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: hacs/action@main
        with:
          category: integration
